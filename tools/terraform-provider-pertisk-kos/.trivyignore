# Prefer the repo-root .trivyignore when scanning from the monorepo root.
# This file is kept so `trivy fs .` / `make trivy` from this directory still works.

# GO-2026-5932 — x/crypto/openpgp only; false positive at module level.
# See ../../.trivyignore for full rationale.
GO-2026-5932
