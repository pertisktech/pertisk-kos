version: v1alpha1
machine:
  type: controlplane
  network:
    hostname: pertisk-cp-1
    interfaces:
      - interface: eth0
        dhcp: true
    nameservers:
      - 1.1.1.1
  # Optional — omit for built-ins (catppuccin / terminal black / ASCII - rules).
  # dashboard:
  #   theme: catppuccin
  #   border: line        # one label-plus-hyphen separator row per section
  #   background: "#1E1E2E" # optional; omit for terminal default black
  #   cols: 93            # only if probe is wrong — wrong size blanks Serial
  #   rows: 25
  #   mgmt_url: https://ptkos.apps.thaidevops.co
  # observability:          # optional — omit disables Loki push
  #   lokiUrl: http://10.1.1.10:3500/loki/api/v1/push
cluster:
  endpoint: https://10.1.1.210:6443
  # From: pertiskctl gen config …
  token: replace.me000000000000
  kubernetesVersion: v1.32.5
  network:
    podSubnets:
      - 10.244.0.0/16
    serviceSubnets:
      - 10.96.0.0/12
  cni: none
