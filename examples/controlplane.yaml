version: v1alpha1
machine:
  type: controlplane
  network:
    hostname: pertisk-cp-1
    interfaces:
      - interface: eth0
        dhcp: true
    nameservers:
      - 1.1.1.1
  # Optional — omit to use built-in defaults (catppuccin / ascii; size from probe).
  # dashboard:
  #   theme: catppuccin
  #   border: ascii
  #   cols: 93          # only if probe is wrong — wrong size blanks Serial
  #   rows: 25
  #   utf8: true
cluster:
  endpoint: https://10.1.1.210:6443
  # From: pertiskctl gen config …
  token: replace.me000000000000
  kubernetesVersion: v1.32.5
  podSubnet: 10.244.0.0/16
  serviceSubnet: 10.96.0.0/12
  cni: none
