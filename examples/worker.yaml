version: v1alpha1
machine:
  type: worker
  network:
    hostname: pertisk-node-1
    interfaces:
      - interface: eth0
        dhcp: true
  # dashboard omitted → built-in defaults (catppuccin / ascii; size from probe)
cluster:
  endpoint: https://10.1.1.210:6443
  token: replace.me000000000000
