# Lab stack: Grafana + Loki (+ Prometheus scrape, Alloy Loki ingest).
# Not for production HA. Do not run this on Pertisk guests.
#
#   cd examples/observability
#   docker compose up -d
#   open http://127.0.0.1:3000   # admin / admin
#
# Point nodes at the compose *host* LAN IP (not localhost, not the container name):
#   machine.observability.lokiUrl: http://10.1.1.15:3500/loki/api/v1/push
# After compose up: ./sync-file-sd.sh  (guest :50001 scrape list from mgmt.db)

name: pertisk-obs

services:
  loki:
    image: grafana/loki:3.4.3
    command: ["-config.file=/etc/loki/config.yaml"]
    volumes:
      - ./compose/loki.yaml:/etc/loki/config.yaml:ro
      - loki-data:/loki
    ports:
      - "3100:3100"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3100/ready || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12

  alloy:
    image: grafana/alloy:v1.7.5
    command:
      - run
      - /etc/alloy/config.alloy
      - --storage.path=/var/lib/alloy
      - --server.http.listen-addr=0.0.0.0:12345
    volumes:
      - ./compose/alloy.alloy:/etc/alloy/config.alloy:ro
    ports:
      - "3500:3500"
    depends_on:
      - loki
    restart: unless-stopped

  pushgateway:
    image: prom/pushgateway:v1.11.1
    command:
      - --persistence.file=/data/pushgateway.data
      - --persistence.interval=1m
    volumes:
      - pushgateway-data:/data
    ports:
      - "9091:9091"
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v3.2.1
    # Host network: Docker bridge cannot reach guest :50001 on the LAN
    # (Loki still works because nodes *push*). Listen on 9990 to keep the
    # published UI port. Grafana uses host.docker.internal:9990.
    network_mode: host
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=7d
      - --web.enable-lifecycle
      - --web.listen-address=0.0.0.0:9990
    volumes:
      - ./compose/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./compose/file_sd:/etc/prometheus/file_sd:ro
      - prometheus-data:/prometheus
    depends_on:
      - pushgateway
    restart: unless-stopped

  grafana:
    image: grafana/grafana:11.5.2
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_AUTH_ANONYMOUS_ENABLED: "false"
      GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH: /var/lib/grafana/dashboards/grafana-node.json
    volumes:
      - ./compose/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana-node.json:/var/lib/grafana/dashboards/grafana-node.json:ro
      - ./grafana-logs.json:/var/lib/grafana/dashboards/grafana-logs.json:ro
      - grafana-data:/var/lib/grafana
    ports:
      - "3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - loki
      - prometheus
      - pushgateway
    restart: unless-stopped

volumes:
  loki-data:
  prometheus-data:
  grafana-data:
  pushgateway-data:
