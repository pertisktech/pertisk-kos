# kubeadm-shaped RBAC so bootstrap-token kubelets can TLS-bootstrap.
# Applied automatically by `pertiskctl bootstrap` once the API is up.
# Keep this file for manual repair / documentation.
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: pertisk:kubelet-bootstrap
subjects:
  - kind: Group
    name: system:bootstrappers:kubeadm:default-node-token
    apiGroup: rbac.authorization.k8s.io
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:node-bootstrapper
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: pertisk:node-autoapprove-bootstrap
subjects:
  - kind: Group
    name: system:bootstrappers:kubeadm:default-node-token
    apiGroup: rbac.authorization.k8s.io
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:certificates.k8s.io:certificatesigningrequests:nodeclient
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: pertisk:node-autoapprove-certificate-rotation
subjects:
  - kind: Group
    name: system:nodes
    apiGroup: rbac.authorization.k8s.io
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:certificates.k8s.io:certificatesigningrequests:selfnodeclient
---
# Serving CSRs are approved by Pertisk bootstrap after validating the node
# identity and signer name. This binding grants the bootstrap admin only.
# Keep this section as documentation for clusters using an external approver.
#
# A kubelet-serving CSR must not be auto-approved solely through system:nodes.
# The bootstrap approver performs the identity and signer checks before patching
# the CSR status.
#
# Apiserver kubelet-client cert CN is `kube-apiserver`. Required for
# kubectl/ktail logs, exec, and port-forward (nodes/proxy).
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: system:kube-apiserver-to-kubelet
subjects:
  - kind: User
    name: kube-apiserver
    apiGroup: rbac.authorization.k8s.io
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:kubelet-api-admin
